import{createHash}from"node:crypto";import{NextResponse}from"next/server";import bcrypt from"bcryptjs";import{z}from"zod";import{supabase}from"@/lib/supabase";
const schema=z.object({password:z.string().min(12).max(128),confirmPassword:z.string().min(12).max(128)}).refine(v=>v.password===v.confirmPassword,{message:"Passwords do not match.",path:["confirmPassword"]});
export async function POST(request:Request,{params}:{params:Promise<{token:string}>}){if(!supabase)return NextResponse.json({error:"Recovery is unavailable."},{status:503});const p=schema.safeParse(await request.json());if(!p.success)return NextResponse.json({error:p.error.issues[0]?.message??"Invalid password."},{status:400});const hash=createHash("sha256").update((await params).token).digest("hex"),passwordHash=await bcrypt.hash(p.data.password,12);const{error}=await supabase.rpc("complete_account_recovery",{p_token_hash:hash,p_password_hash:passwordHash});if(error)return NextResponse.json({error:"This recovery link is invalid or expired."},{status:409});return NextResponse.json({ok:true});}
