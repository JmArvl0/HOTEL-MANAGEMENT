import{NextResponse}from"next/server";import{canExecuteManagerFinancialApproval}from"@/lib/permissions";import{guardManager,managerGuardFailed,managerRpcFailure}from"@/lib/manager-route";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const context=await guardManager(canExecuteManagerFinancialApproval,"Accounting execution authority required.");if(managerGuardFailed(context))return context;const{data,error}=await context.client.rpc("accounting_execute_manager_financial_approval",{p_approval_id:(await params).id,p_staff_user_id:context.actorId});if(error)return managerRpcFailure(error,{APPROVAL_NOT_EXECUTABLE:"This approval is not awaiting Accounting execution.",COMPENSATION_EXCEEDS_FOLIO:"The approved compensation is no longer valid for the current folio.",FOLIO_NOT_FOUND:"The reservation folio is unavailable."},"Unable to execute this approved financial exception.");return NextResponse.json({data})}