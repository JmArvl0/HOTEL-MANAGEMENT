import{getServerSession}from"next-auth";import{NextResponse}from"next/server";import{authOptions}from"@/lib/auth";import{supabase}from"@/lib/supabase";
const allowed=new Set(["owner","admin","front_desk"]);export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const session=await getServerSession(authOptions);if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});if(!allowed.has(session.user.role))return NextResponse.json({error:"Front Desk access required."},{status:403});if(!supabase)return NextResponse.json({error:"Database unavailable."},{status:503});const{error}=await supabase.rpc("verify_guest_identity",{p_reservation_id:(await params).id,p_staff_user_id:session.user.id});if(error)return NextResponse.json({error:"Identity verification is not permitted for this reservation."},{status:409});return NextResponse.json({ok:true})}
