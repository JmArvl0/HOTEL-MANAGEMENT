import Link from"next/link";import{notFound}from"next/navigation";import{requireCustomerSession}from"@/lib/customer-auth";import{supabase}from"@/lib/supabase";import{formatPeso}from"@/lib/booking";import{friendlyStatus}from"@/lib/customer";
export default async function CustomerReceiptPage({params}:{params:Promise<{id:string}>}){const session=await requireCustomerSession();const{id}=await params;if(!supabase)notFound();const{data:payment}=await supabase.from("payments").select("id,reservation_id,amount,currency,method,reference,purpose,status,verified_at,created_at").eq("id",id).eq("status","paid").maybeSingle();if(!payment)notFound();const{data:reservation}=await supabase.from("reservations").select("id,confirmation_number,guest_name,room_type,check_in,check_out,user_id").eq("id",payment.reservation_id).eq("user_id",session.user.id).maybeSingle();if(!reservation)notFound();return <section className="customer-detail-card customer-receipt"><p className="eyebrow">Official payment record</p><h1>Payment receipt</h1><p>HAVEN Hotel &amp; Residences</p><dl><div><dt>Receipt reference</dt><dd>{payment.id}</dd></div><div><dt>Reservation</dt><dd>{reservation.confirmation_number??reservation.id}</dd></div><div><dt>Guest</dt><dd>{reservation.guest_name}</dd></div><div><dt>Stay</dt><dd>{reservation.check_in} to {reservation.check_out}</dd></div><div><dt>Payment</dt><dd>{friendlyStatus(payment.purpose)}</dd></div><div><dt>Method</dt><dd>{friendlyStatus(payment.method)}</dd></div><div><dt>Provider reference</dt><dd>{payment.reference??"—"}</dd></div><div><dt>Verified</dt><dd>{new Date(payment.verified_at??payment.created_at).toLocaleString("en-PH")}</dd></div><div className="detail-balance"><dt>Amount</dt><dd>{formatPeso(payment.amount)}</dd></div></dl><p>This receipt is generated from Haven&apos;s settled, immutable payment record.</p><Link className="customer-inline-action" href="/account/payments">Back to payments</Link></section>}