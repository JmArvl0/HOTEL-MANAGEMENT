import{BookOpenText}from"lucide-react";import{requireCustomerSession}from"@/lib/customer-auth";import{getGuestReservations}from"@/lib/booking";import{friendlyStatus,getCustomerRequests}from"@/lib/customer";import{GuestRequestForm}from"@/components/customer/guest-request-form";import{StatusBadge}from"@/components/ui";
export default async function RequestsPage(){const session=await requireCustomerSession();const[requests,reservations]=await Promise.all([getCustomerRequests(session.user.id),getGuestReservations(session.user.id)]);const eligible=reservations.filter((item)=>["confirmed","checked_in"].includes(String(item.status))).map((item)=>({id:item.id,confirmation_number:item.confirmation_number,room_type:item.room_type}));return <><section className="customer-page-title split"><div><p className="eyebrow">Guest requests</p><h1>How can we help?</h1><p>Tell us what you need; Haven will route it to the right team.</p></div><GuestRequestForm reservations={eligible}/></section>{!requests.length?<div className="customer-empty"><BookOpenText/><h2>No requests yet</h2><p>Your service request history will appear here.</p></div>:<div className="customer-request-list">{requests.map((request)=><article key={request.id}><div><StatusBadge status={request.status} size="sm" /><h2>{request.request}</h2><p>{request.reservation?.confirmation_number??request.reservation_id} · {request.reservation?.room_type}</p></div><time>{new Date(request.created_at).toLocaleString("en-PH",{dateStyle:"medium",timeStyle:"short"})}</time></article>)}</div>}</>}