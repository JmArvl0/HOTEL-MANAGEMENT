import Link from"next/link";import{Sparkles}from"lucide-react";import{RegisterForm}from"@/components/auth/register-form";import{safeInternalPath}from"@/lib/booking";
export default async function RegisterPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const params=await searchParams;const callbackUrl=safeInternalPath(params.callbackUrl,"/auth/continue");return <main className="login-page"><section className="login-art"><Link href="/" className="brand brand-light"><span className="brand-mark"><Sparkles size={18}/></span><span>HAVEN<small>HOTEL & RESIDENCES</small></span></Link><div><p className="eyebrow light">Your Haven account</p><h1>A beautiful stay<br/><em>starts here.</em></h1><p>Create your guest account to keep reservations secure and easy to manage.</p></div><small>© 2026 Haven Hotel & Residences</small></section><section className="login-form-wrap"><div className="login-form"><Link className="back-link" href={`/login?callbackUrl=${encodeURIComponent(callbackUrl)}`}>← Back to sign in</Link><p className="eyebrow">Guest registration</p><h2>{params.booking==="1"?"Create an account to continue":"Join Haven."}</h2><p>Your booking context will be preserved after registration.</p><RegisterForm callbackUrl={callbackUrl}/></div></section></main>}