"use client";
import{FormEvent,useState}from"react";import{useSession}from"next-auth/react";import{CheckCircle2,KeyRound,Save}from"lucide-react";
export function ProfileForm({defaults}:{defaults:{name:string;email:string;mobile:string;address:string;nationality:string}}){const{update}=useSession();const[message,setMessage]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setMessage("");setError("");const response=await fetch("/api/account/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});const body=await response.json();setLoading(false);if(!response.ok){setError(body.error??"Unable to update your profile.");return}await update({name:String(Object.fromEntries(new FormData(event.currentTarget)).name)});setMessage("Profile updated successfully.")}return <form className="account-form" onSubmit={submit}><div className="account-form-grid"><label>Full name<input name="name" defaultValue={defaults.name} required maxLength={120}/></label><label>Email<input value={defaults.email} disabled/><small>Email changes require Front Desk assistance.</small></label><label>Mobile number<input name="mobile" defaultValue={defaults.mobile} maxLength={30}/></label><label>Nationality<input name="nationality" defaultValue={defaults.nationality} maxLength={80}/></label><label className="wide">Address<textarea name="address" defaultValue={defaults.address} maxLength={300} rows={3}/></label></div>{error&&<p className="booking-error" role="alert">{error}</p>}{message&&<p className="account-success"><CheckCircle2 size={16}/>{message}</p>}<button className="btn btn-accent" disabled={loading}><Save size={16}/>{loading?"Saving…":"Save profile"}</button></form>}
export function PasswordForm(){const[message,setMessage]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setMessage("");setError("");const form=event.currentTarget;const values=Object.fromEntries(new FormData(form));if(values.newPassword!==values.confirmPassword){setLoading(false);setError("New passwords do not match.");return}const response=await fetch("/api/account/password",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(values)});const body=await response.json();setLoading(false);if(!response.ok){setError(body.error??"Unable to change your password.");return}form.reset();setMessage("Password changed successfully.")}return <form className="account-form" onSubmit={submit}><div className="account-form-grid single"><label>Current password<input name="currentPassword" type="password" autoComplete="current-password" required/></label><label>New password<input name="newPassword" type="password" minLength={8} autoComplete="new-password" required/></label><label>Confirm new password<input name="confirmPassword" type="password" minLength={8} autoComplete="new-password" required/></label></div>{error&&<p className="booking-error" role="alert">{error}</p>}{message&&<p className="account-success"><CheckCircle2 size={16}/>{message}</p>}<button className="btn btn-accent" disabled={loading}><KeyRound size={16}/>{loading?"Updating…":"Change password"}</button></form>}