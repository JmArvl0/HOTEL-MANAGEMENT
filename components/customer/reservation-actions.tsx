"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export function ReservationActions({id,status}:{id:string;status:string}){const router=useRouter();const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");if(!["pending","confirmed"].includes(status))return null;
async function cancel(){const reason=window.prompt("Why are you cancelling this reservation?");if(!reason)return;if(!window.confirm("Cancel this reservation? Refund eligibility will be calculated from the policy accepted when you booked."))return;setBusy(true);const response=await fetch(`/api/account/reservations/${id}/cancel`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({reason})});const body=await response.json();setBusy(false);if(!response.ok){setMessage(body.error??"Unable to cancel.");return}const refund=Number(body.data?.eligible_refund??0);setMessage(refund>0?`Cancelled. PHP ${refund.toLocaleString("en-PH")} is awaiting Accounting refund processing.`:"Cancelled. The booking-time policy provides no deposit refund.");router.refresh()}
async function change(){const checkIn=window.prompt("Requested new check-in date (YYYY-MM-DD), or leave blank:")??"";const checkOut=window.prompt("Requested new check-out date (YYYY-MM-DD), or leave blank:")??"";const roomType=window.prompt("Requested room type, or leave blank:")??"";if(!checkIn&&!checkOut&&!roomType)return;const reason=window.prompt("Reason for the requested change:");if(!reason)return;setBusy(true);const response=await fetch(`/api/account/reservations/${id}/change-request`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({checkIn:checkIn||undefined,checkOut:checkOut||undefined,roomType:roomType||undefined,reason,idempotencyKey:crypto.randomUUID()})});const body=await response.json();setBusy(false);setMessage(response.ok?(body.data?.status==="executed"?`Reservation updated. New total: PHP ${Number(body.data.calculatedTotal).toLocaleString("en-PH")}.`:"Your request requires Manager approval. Front Desk will execute it after approval."):body.error??"Unable to request changes.");if(response.ok)router.refresh()}
return <div className="customer-reservation-actions"><button className="btn btn-soft" disabled={busy} onClick={change}>Request a change</button><button className="btn btn-soft danger-action" disabled={busy} onClick={cancel}>Cancel reservation</button>{message&&<p role="status">{message}</p>}</div>}
