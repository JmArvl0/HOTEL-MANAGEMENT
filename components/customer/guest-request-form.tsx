"use client";
import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{Send}from"lucide-react";
export function GuestRequestForm({reservations}:{reservations:{id:string;confirmation_number:string|null;room_type:string}[]}){const router=useRouter();const[open,setOpen]=useState(false);const[requestType,setRequestType]=useState("extra_towels");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const values=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/account/requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...values,idempotencyKey:crypto.randomUUID()})});const body=await response.json();setLoading(false);if(!response.ok){setError(body.error??"Unable to submit your request.");return}setOpen(false);router.refresh()}if(!reservations.length)return <p className="customer-empty-note">A confirmed or current reservation is required before submitting a guest request.</p>;return <>{!open?<button className="btn btn-accent" onClick={()=>setOpen(true)}>New request</button>:<form className="customer-request-form" onSubmit={submit}><label>Reservation<select name="reservationId" required>{reservations.map((reservation)=><option value={reservation.id} key={reservation.id}>{reservation.confirmation_number??reservation.id} · {reservation.room_type}</option>)}</select></label><label>What can we help with?<select name="requestType" required value={requestType} onChange={(event)=>setRequestType(event.target.value)}><option value="extra_towels">Extra towels</option><option value="toiletries">Toiletries</option><option value="housekeeping">Housekeeping</option><option value="maintenance">Maintenance concern</option><option value="room_assistance">Room assistance</option><option value="room_change">Request a room change</option><option value="stay_extension">Request a stay extension</option><option value="general">General hotel assistance</option></select></label>{requestType==="stay_extension"&&<label>Requested new check-out<input name="requestedCheckOut" type="date" required/></label>}<label>Details<textarea name="description" rows={4} minLength={3} maxLength={500} required placeholder="Tell us what you need and any useful details."/></label>{error&&<p className="booking-error" role="alert">{error}</p>}<div><button type="button" className="btn btn-soft" onClick={()=>setOpen(false)}>Cancel</button><button className="btn btn-accent" disabled={loading}><Send size={15}/>{loading?"Submitting…":"Submit request"}</button></div></form>}</>}