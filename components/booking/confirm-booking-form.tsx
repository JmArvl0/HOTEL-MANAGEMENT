"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { Landmark, ShieldCheck, Smartphone } from "lucide-react";
import { formatPeso } from "@/lib/format";
export function ConfirmBookingForm({token,deposit}:{token:string;deposit:number}){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch(`/api/booking/holds/${token}/confirm`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({paymentMethod:form.get("paymentMethod"),paymentReference:form.get("paymentReference")})});const body=await response.json();setLoading(false);if(!response.ok){setError(body.error??"We could not submit the reservation deposit.");return}router.push(`/booking/confirmation/${body.reservationId}`);router.refresh()}return <form className="booking-form-card deposit-form" onSubmit={submit}><fieldset><legend>How did you send the reservation deposit?</legend><label className="choice"><input type="radio" name="paymentMethod" value="manual_gcash" defaultChecked/><Smartphone/><span><strong>GCash transfer</strong><small>Use the transaction reference from your completed transfer.</small></span></label><label className="choice"><input type="radio" name="paymentMethod" value="manual_bank_transfer"/><Landmark/><span><strong>Bank transfer</strong><small>Use the bank confirmation or transfer reference.</small></span></label></fieldset><label className="deposit-reference">Payment reference<input name="paymentReference" required minLength={4} maxLength={120} autoComplete="off" placeholder="Enter your transfer reference"/></label><p className="deposit-verification-note">Haven does not currently have an automated payment gateway. Submitting this reference creates a pending payment for staff verification; it does not mark the deposit as paid.</p>{error&&<p className="booking-error" role="alert">{error}</p>}<button className="btn btn-accent" disabled={loading}><ShieldCheck size={17}/>{loading?"Submitting...":`Submit ${formatPeso(deposit)} Deposit for Verification`}</button></form>}
