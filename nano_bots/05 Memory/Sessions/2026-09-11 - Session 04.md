# 2026-09-11 — Session 04: Landing hero/wash scroll sync + Featured Stays alignment

## Work completed

User reported two landing regressions at `localhost:3000`: hero image scaling past a fixed
hero wash during scroll, and Featured Stays cards misaligning. **The report matched the
committed HEAD code** (GSAP scaled `.coast-hero-image` while `.coast-hero-wash` stayed
sized to the hero). The **uncommitted working tree already contained a parallel session's
complete fix** (Option A, exactly per spec); this session verified it, closed the remaining
audit gaps, and pinned the architecture:

Verified pre-existing fix (parallel session, previously undocumented):
- `app/(landing-page)/page.tsx` — image + wash nested in `div.coast-hero-media`.
- `app/(landing-page)/landing.css` — `.coast-hero` owns `overflow:hidden`;
  `.coast-hero-media` (absolute, inset 0, transform-origin center) owns geometry + scale;
  no transform on image/wash themselves. Featured Stays: grid `align-items:stretch`,
  cards flex-column `height:100%`, price row `margin-top:auto`.
- `components/landing/landing-motion.tsx` — GSAP scales the wrapper (load 1.05→1, scrub
  `fromTo` 1→1.12 `immediateRender:false`); room grid reveals as one unit (per-card
  stagger removed).

This session's additions:
- `.coast-hero-wash` gained `pointer-events:none` (decorative overlay contract).
- `components/landing/landing-hero-structure.test.tsx` (new) — pins the wrapper nesting,
  wrapper-as-scale-owner (source + CSS), hero clipping, and the card-alignment layout
  rules so the regression class can't silently return (5 tests).
- `docs/ui-motion-guidelines.md` §6 — documented the ownership rule: `.coast-hero-media`
  is the single scale owner; never animate the bare image or wash independently.
- Confirmed the dev server on :3000 already serves the wrapper (curl of the RSC payload),
  so the user's observation was a cached pre-fix page — hard reload resolves it.

Unchanged: GSAP timings/ownership, booking form/query behavior, room-discovery intents
(focused/browse/availability), reduced-motion contract, responsive breakpoints, content.

## Verification

- `:3000` served-page check — `coast-hero-media` present wrapping image + wash
- `npm run typecheck` — clean
- `npm run lint` — 0 errors (71 pre-existing warnings)
- `npx vitest run components/landing` — 12/12 (nav, gallery, new pin test)
- `npm test` — 743/743 (63 files)
- `npm run build` — compiled successfully
- Browser TEST 1–8 (hero sync through slow/reverse/rapid scroll, resize, card alignment,
  1440/1024/768/390, reduced-motion) pending on user side with a hard reload
  (Ctrl+Shift+R) — no browser automation available in this session.

## Recommended next action

User-side visual pass per the TEST 1–8 checklist after a hard reload of `/`. If any desync
persists after the reload, capture the URL and viewport — but the served DOM and the
motion/CSS audit both confirm one composited media system.
