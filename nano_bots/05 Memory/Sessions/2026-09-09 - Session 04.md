# 2026-09-09 - Session 04 — Coastal landing + system-wide palette completion

## Objective

User's Codex session hit its usage limit mid-implementation of the coastal/bright
landing redesign (user consented to changing the color of the entire system to match
reference pictures). This team finished the work.

## User rulings

- Location copy: **neutral / no address** — the Codex copy's fictional "Mactan Bay,
  Lapu-Lapu City, Cebu" was removed; no street/city anywhere; phone/email/maps kept.
- Room-catalog test conflict (qa escalation): **update the test, not the page** — the
  Codex rewrite (inline `Intl.NumberFormat` pricing, `coast-catalog-empty` state instead
  of hardcoded fallback rooms) satisfies the real contract (DB-driven live rates,
  `revalidate = 300`); `lib/room-catalog.test.ts` assertions updated to match.
- Landing motion: **delete** `components/landing/landing-motion.tsx` — dead code (its
  `[data-reveal]` CSS layer was never written into the rewritten landing.css).

## What was already done by Codex (verified, not redone)

Coastal landing page (`app/(landing-page)/page.tsx` + `landing.css`), shared override
layer `app/coastal-theme.css` (imported last in layout), landing components
(landing-nav with accessible drawer, wave-mark, experience-gallery).

## Changes (this team — all display-layer; no lib/, no schema, no new deps)

- **Neutral copy** — hero, contact section, auth-vault label de-located
  (frontend).
- **Palette completion** — old forest `#173f32` / terracotta `#c9783c` / `#1f6b52` /
  `#ecd0b4` retired everywhere: manager-dashboard-theme.css, guest-booking.css,
  globals.css (`:root` source tokens + warm login tints), design-tokens.css (brand
  primitives), auth surfaces (login.css, auth-vault.css, login/register-form inline
  styles), Modal.css fallback, haven-loader.css, booking search.css, manager chart
  colors (recharts strokes/fills), and the favicon `app/icon.svg`. Semantic status
  families (badges, danger, paid/partial/unpaid) untouched.
- **Dark-mode parity** — `coastal-theme.css` gained a `@media (prefers-color-scheme:
  dark) :root:not(.theme-light)` deep-teal remap (covers auth/public dark; landing
  stays light-only).
- **Design review fixes** (uiux) — search hero `em` tint → `#c2e0dc`; rooms-card
  `.badge.available` normalized to the shell green pair.
- **Docs** — `DESIGN.md` (cascade table row 9, coastal palette, terracotta retired) +
  `DESIGN-STATUS.md` entry (uiux).
- **Tests** — new `components/landing/experience-gallery.test.tsx` (3) +
  `landing-nav.test.tsx` (4); `lib/room-catalog.test.ts` landing assertions updated.

## Verification

typecheck clean, lint 0 errors (72 baseline warnings), full suite 683/685 (only the
known manager-reservations-panel date-rot ×2), build green 57/57. Ripgrep: zero
old-brand hexes in app/ + components/ (sole holdover: design-tokens.css
`--brand-accent-dark` — confirmed dead, coastal-theme overrides it).

## Flagged, not fixed (user's call)

- coast-hero-note white italic over uncontrolled photo — contrast not guaranteed.
- Booking-bar labels at 9px — small for a primary conversion control.
- Light-mode unpaid vs partial amber badges sit close (pre-existing).

## Open (unchanged rulings)

Guest-side audit rows for hold/confirm RPCs; TomTom removal confirmation;
manager-reservations-panel date-rot fixture fix (qa recommends deriving fixture dates
from runtime clock); commit of the large uncommitted tree (now even larger).
