# 2026-09-09 - Session 03 — Booking process data-placement audit + fixes

## Objective

User reported booking-process data "not going where it is supposed to be" and
"parts that are needed to be in this place missing." Full read-only audit of the
booking flow, then display-layer fixes.

## Audit outcome

The booking spine (search → details → hold → proof → deposit → Accounting
verification → confirmation, including the auto-filed guest_request batch and
transportation_requests row) matches SYSTEM.md and is sound. All defects were
display-layer dead-ends — data collected, stored, even queried, then dropped
before reaching a screen:

| Field | Collected | Stored | Gap |
|---|---|---|---|
| Address / nationality | guest-details-form | `guests` (upsert in `create_booking_hold`) | queried in `lib/customer.ts` + never rendered anywhere; staff guests select omitted them |
| `request_options` | guest-details-form | `reservations` | staff detail modal/`operationalReservationFields` never selected it — pending reservations showed nothing until confirmation trigger filed `guest_requests` |
| `expected_arrival` | guest-details-form | `reservations` | guest my-reservations detail dropped it at page mapping |
| Search-page copy | — | — | "Pay at hotel" + "Mactan Bay" contradicted the 30%-deposit flow and Haven Makati brand |

## Changes (display-layer only, no schema/RPC/business-rule changes)

- `lib/staff-data.ts` — `operationalReservationFields` + `getStaffReservation` reservation/guests selects gain `request_options`, `nationality`, `address` (Accounting's slim projection deliberately unchanged).
- `components/manager/manager-dashboard-client.tsx` — `ReservationDetailModal`: Nationality + Address rows in Guest contact; "Requested at booking" chips in Requests and notes via `requestLabel`.
- `app/globals.css` — `.detail-request-chips` / `.chip-row` rules beside the existing reservation-detail block.
- `components/customer/reservation-detail-view.tsx` + `app/(booking)/(customer)/my-reservations/[id]/page.tsx` — `expectedArrival` passed through and rendered in Stay details.
- `app/(booking)/booking/review/[token]/page.tsx` — address + nationality shown before deposit submission.
- `app/(booking)/booking/search/page.tsx` — "Makati" kicker; deposit-accurate sidebar copy.
- `lib/staff-reservations.test.ts` — new RBAC assertions: `request_options` in operational fields; Accounting projection stays slim.

## Verification

typecheck clean, lint 0 errors (72 pre-existing warnings), vitest 673/675 — only
the pre-existing date-rot failures in `manager-reservations-panel.test.tsx`
(deterministic in isolation, qa-characterized), build green.

## Open (unchanged rulings)

Landing-page FAQ "pay at hotel" copy; guest-side audit rows for hold/confirm RPCs;
TomTom removal confirmation; manager-reservations-panel date-rot fix; commit of the
large uncommitted tree.
